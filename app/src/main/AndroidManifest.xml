<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.caiocardozo.linguagensprogramacaokotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeCaio">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ItalianoActivity" />
        <activity android:name=".BrasileiroActivity" />
        <activity android:name=".AlemaoActivity" />
        <activity android:name=".InglesActivity" />
        <activity android:name=".EspanholActivity"></activity>
    </application>

</manifest>